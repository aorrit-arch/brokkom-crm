# Configuració Netlify per a Brokkom CRM

[build]
  publish = "."
  functions = "netlify/functions"

# Redirecció més curta per a la function (opcional)
[[redirects]]
  from = "/api/anthropic"
  to = "/.netlify/functions/anthropic-proxy"
  status = 200
